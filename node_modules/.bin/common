#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../common-style/bin/styleguide" "$@"
  ret=$?
else 
  node  "$basedir/../common-style/bin/styleguide" "$@"
  ret=$?
fi
exit $ret

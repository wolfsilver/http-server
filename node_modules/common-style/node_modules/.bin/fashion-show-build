#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../fashion-show/bin/build" "$@"
  ret=$?
else 
  node  "$basedir/../fashion-show/bin/build" "$@"
  ret=$?
fi
exit $ret
